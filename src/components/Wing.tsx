import { Component, FC, ReactNode } from "react"

const Wing = ({content}: {content: ReactNode}) => {
    return (
        <div>
            {content}
        </div>
    )
}

export default Wing