import { FC } from "react"

const AccessibilityLeft = () => {
    return (
        <div className="accessibility">
            <button>right</button>
            <select>
                <option>light</option>
                <option>dark</option>
            </select>
        </div>
    )
}

export default AccessibilityLeft